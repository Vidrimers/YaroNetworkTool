# Быстрое развертывание VPN Management System

## Сценарий: API на сервере, бот локально

### Часть 1: Развертывание API на сервере (144.124.237.222)

#### Шаг 1: Подключиться к серверу

```bash
ssh root@144.124.237.222
```

#### Шаг 2: Установить необходимые компоненты

```bash
# Обновить систему
apt update && apt upgrade -y

# Установить Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# Установить PM2
npm install -g pm2

# Установить Git и SQLite3
apt install -y git sqlite3

# Проверить версии
node --version    # должно быть >= 18.0.0
npm --version
pm2 --version
```

#### Шаг 3: Клонировать репозиторий

```bash
# Перейти в домашнюю директорию
cd ~

# Клонировать репозиторий (замените URL на ваш)
git clone https://github.com/YOUR_USERNAME/xray-vpn-server.git xray-vpn

# Перейти в директорию
cd xray-vpn

# Установить зависимости
npm install --production
```

#### Шаг 4: Настроить .env

```bash
# Создать .env файл
nano .env
```

Вставьте:
```env
# API Configuration
API_PORT=3000
NODE_ENV=production

# Database
DB_PATH=~/xray-vpn/database/vpn.db

# JWT Secret (сгенерируйте случайную строку)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Server
SERVER_IP=144.124.237.222
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

```bash
# Установить права
chmod 600 .env
```

#### Шаг 5: Инициализировать базу данных

```bash
# Создать директорию
mkdir -p ~/xray-vpn/database

# Инициализировать базу данных
sqlite3 ~/xray-vpn/database/vpn.db < database/init.sql

# Проверить что таблицы созданы
sqlite3 ~/xray-vpn/database/vpn.db "SELECT name FROM sqlite_master WHERE type='table';"
# Должны появиться: clients, extension_requests, traffic_logs
```

#### Шаг 6: Запустить API через PM2

```bash
cd ~/xray-vpn

# Запустить API
pm2 start api/server.js --name vpn-api --time

# Проверить статус
pm2 status

# Просмотреть логи
pm2 logs vpn-api --lines 50

# Сохранить конфигурацию для автозапуска
pm2 save
pm2 startup
# Выполните команду которую выведет pm2 startup
```

#### Шаг 7: Проверить работу API

```bash
# Health check
curl http://localhost:333/health
# Должен вернуть: {"status":"ok",...}

# Список клиентов (должен быть пустой)
curl http://localhost:333/api/v1/clients
# Должен вернуть: {"success":true,"count":0,"clients":[]}
```

#### Шаг 8: Настроить файрвол (опционально)

```bash
# Разрешить SSH и X-Ray порты
ufw allow 22/tcp      # SSH
ufw allow 443/tcp     # X-Ray (HTTPS)
ufw enable

# API доступен только локально (localhost:333)
# Бот будет подключаться через SSH туннель или напрямую если разрешить порт
```

#### Шаг 9: Сделать скрипты исполняемыми

```bash
chmod +x ~/xray-vpn/scripts/kvn.sh
chmod +x ~/xray-vpn/scripts/kvn-stop.sh
```

✅ **API на сервере готов!**

---

### Часть 2: Запуск бота локально (на вашем компьютере)

#### Шаг 1: Установить Node.js (если еще не установлен)

**Windows:**
- Скачайте с https://nodejs.org/ (версия 18+)
- Установите

**Проверка:**
```bash
node --version
npm --version
```

#### Шаг 2: Перейти в директорию бота

```bash
cd yaronetworktool
```

#### Шаг 3: Установить зависимости

```bash
npm install
```

#### Шаг 4: Настроить .env для локального запуска

Откройте `yaronetworktool/.env` и проверьте/обновите:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=UR_TOKEN

TELEGRAM_ADMIN_ID=137981675

# API Configuration (подключение к серверу)
API_BASE_URL=http://144.124.237.222:333
SERVER_IP=144.124.237.222

# Database (используем API, не прямой доступ к БД)
DB_PATH=./yaronetworkbase.db

# SSH Configuration (для прямого доступа к серверу, если нужно)
SSH_HOST=144.124.237.222
SSH_PORT=22
SSH_USER=root
SSH_PASSWORD=your-server-password
```

**Важно:** 
- Если API порт 3000 закрыт файрволом, нужно либо открыть его (`ufw allow 3000/tcp`), либо использовать SSH туннель (см. ниже)

#### Шаг 5: Вариант A - Открыть порт API на сервере (проще)

На сервере:
```bash
ufw allow 333/tcp
ufw reload
```

Теперь API доступен по `http://144.124.237.222:333`

#### Шаг 5: Вариант B - Использовать SSH туннель (безопаснее)

На локальном компьютере:
```bash
# Создать SSH туннель (в отдельном терминале)
ssh -L 333:localhost:333 root@144.124.237.222

# Оставьте этот терминал открытым
```

В `.env` измените:
```env
API_BASE_URL=http://localhost:333
```

#### Шаг 6: Запустить бота локально

```bash
cd yaronetworktool
node bot/yaronetworktool_bot.js
```

Вы должны увидеть:
```
[YaroNetworkTool VPN Bot] Запущен
Admin ID: 137981675
Server IP: 144.124.237.222
```

#### Шаг 7: Проверить работу бота

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должно появиться админ меню

✅ **Бот работает локально!**

---

## Тестирование системы

### Тест 1: Создать клиента через бота

1. В Telegram отправьте `/add_client`
2. Введите имя: `Test User`
3. Введите Telegram ID: `0` (или ваш ID)
4. Введите дни: `30`
5. Должно появиться сообщение с UUID

### Тест 2: Проверить через API

На сервере:
```bash
curl http://localhost:3000/api/v1/clients
```

Должен появиться созданный клиент.

### Тест 3: Запрос на продление

1. Обновите telegram_id клиента в базе (на сервере):
```bash
sqlite3 ~/xray-vpn/database/vpn.db
UPDATE clients SET telegram_id = YOUR_TELEGRAM_ID WHERE name = 'Test User';
.quit
```

2. От имени клиента нажмите `🔑 Запросить ключ`
3. Выберите период (например, 3 месяца)
4. Администратор должен получить уведомление
5. Нажмите `✅ Разрешить`
6. Клиент получит уведомление об одобрении

---

## Полезные команды

### На сервере:

```bash
# Проверить статус API
pm2 status
pm2 logs vpn-api

# Перезапустить API
pm2 restart vpn-api

# Остановить API
pm2 stop vpn-api

# Обновить API из Git
cd ~/xray-vpn && ./scripts/kvn.sh

# Проверить базу данных
sqlite3 ~/xray-vpn/database/vpn.db
SELECT * FROM clients;
.quit
```

### Локально (бот):

```bash
# Запустить бота
cd yaronetworktool
node bot/yaronetworktool_bot.js

# Остановить бота
Ctrl+C

# Проверить подключение к API
curl http://144.124.237.222:3000/health
# или через туннель:
curl http://localhost:3000/health
```

---

## Troubleshooting

### Бот не подключается к API

**Проблема:** `Error: connect ECONNREFUSED`

**Решение:**
1. Проверьте что API запущен на сервере: `pm2 status`
2. Проверьте порт в файрволе: `ufw status`
3. Проверьте API_BASE_URL в `.env`
4. Попробуйте использовать SSH туннель (Вариант B)

### API не запускается

**Проблема:** PM2 показывает статус "errored"

**Решение:**
```bash
pm2 logs vpn-api --lines 100
# Проверьте ошибки в логах
```

Частые причины:
- Порт 333 уже занят: `netstat -tulpn | grep 333`
- Неправильный путь к БД в .env
- База данных не инициализирована

### База данных не найдена

**Решение:**
```bash
cd ~/xray-vpn
sqlite3 ~/xray-vpn/database/vpn.db < database/init.sql
pm2 restart vpn-api
```

---

## Следующие шаги

После успешного тестирования:

1. **Развернуть бота на сервере** (опционально):
   - Следуйте инструкциям в DEPLOYMENT-GUIDE.md
   - Запустите бота через PM2 на сервере

2. **Настроить автоматические бэкапы**:
   ```bash
   crontab -e
   # Добавить:
   0 3 * * * cp ~/xray-vpn/database/vpn.db ~/xray-vpn-backup/vpn_$(date +\%Y\%m\%d).db
   ```

3. **Интегрировать с X-Ray Core**:
   - Установить X-Ray Core на сервере
   - Настроить конфигурации
   - Использовать CLI скрипты для управления клиентами

---

**Полная документация:** [DEPLOYMENT-GUIDE.md](DEPLOYMENT-GUIDE.md)
