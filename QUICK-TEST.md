# Быстрый запуск для тестирования

## Шаг 1: Запуск Management API

```bash
# В корне первого репозитория
npm install

# Инициализировать базу данных
sqlite3 database/vpn.db < database/init.sql

# Запустить API
node api/server.js
```

API запустится на `http://localhost:3000`

## Шаг 2: Запуск Telegram бота

```bash
# В папке yaronetworktool
cd yaronetworktool
npm install

# Проверить .env (должны быть TELEGRAM_BOT_TOKEN и TELEGRAM_ADMIN_ID)
cat .env

# Запустить бота
node bot/yaronetworktool_bot.js
```

## Шаг 3: Тестирование

### Тест от имени администратора

1. Отправьте `/start` боту - должно появиться админ меню
2. Отправьте `/add_client`:
   - Имя: `Test User`
   - Telegram ID: `0` (или ваш ID)
   - Дни: `30`
3. Отправьте `/list_clients` - должен появиться созданный клиент
4. Нажмите кнопку `📊 Статистика` - должна появиться статистика

### Тест от имени клиента

1. Обновите telegram_id в базе:
   ```bash
   sqlite3 database/vpn.db
   UPDATE clients SET telegram_id = YOUR_TELEGRAM_ID WHERE name = 'Test User';
   .quit
   ```

2. Отправьте `/start` боту - должно появиться клиентское меню
3. Нажмите `📊 Мой VPN` - должна появиться статистика
4. Нажмите `🔑 Запросить ключ` → выберите `3 месяца`
5. Администратор получит уведомление с кнопками
6. Нажмите `✅ Разрешить` - подписка продлится на 90 дней
7. Клиент получит уведомление об одобрении

## Проверка работы API

```bash
# Список клиентов
curl http://localhost:3000/api/v1/clients

# Список запросов
curl http://localhost:3000/api/v1/extension-requests
```

## Что работает

✅ Добавление/удаление клиентов через бота
✅ Просмотр списка клиентов и статистики
✅ Запросы на продление от клиентов
✅ Одобрение/отказ/изменение периода админом
✅ Автоматическое продление подписки
✅ Уведомления клиентам о решении
✅ Интеграция с Management API (22 endpoints)

## Что еще не реализовано

⏳ Генерация vless:// ссылок (требует CLI скрипты на VPN сервере)
⏳ Мониторинг трафика в реальном времени (требует парсер логов X-Ray)
⏳ Автоматическая блокировка при превышении лимитов (требует cron задачи)
⏳ Резервное копирование (требует скрипты бэкапа)

## Troubleshooting

**Бот не отвечает:**
- Проверьте TELEGRAM_BOT_TOKEN в yaronetworktool/.env
- Проверьте логи бота

**API не работает:**
- Проверьте: `curl http://localhost:3000/health`
- Проверьте что API запущен

**База данных не найдена:**
- Инициализируйте: `sqlite3 database/vpn.db < database/init.sql`

Подробная документация: `yaronetworktool/docs/TESTING.md`
